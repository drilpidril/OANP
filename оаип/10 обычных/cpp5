5.	Ввод элементов массива через индексы
int b[10]; // объявление массива 
for (int i = 0; i < 10; i++) // ввод элементов массива 
{
 	cout << "b[" << i << "] = ";
 cin >> b[i]; 
}
программа запрашивает у пользователя 10 целых чисел и сохраняет их в массив 
For (int i = 0; i < 10; i++)   // ввод элементов массива
{
    Cout << “b[“ << i << “] = “;
    Cin >> b[i];
}
```

Что делает:
Заполняет массив b из 10 целых чисел, вводя каждый элемент с клавиатуры.
