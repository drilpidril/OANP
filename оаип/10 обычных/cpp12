12.	Обработка массива: замена отрицательных на ноль

```cpp
#include <iostream>
Using namespace std;
Int main()
{
    Int a[20];  // объявляем статический массив, задав максимальную размерность
    Int n;
    Cout << “n = “;
    Cin >> n;   // ввели количество элементов массива
    For (int i = 0; i < n; ++i)
    {
        Cout << “a[“ << i << “] = “;
        Cin >> a[i];
        If (a[i] < 0)
            A[i] = 0;
    }
    For (int i = 0; i < n; ++i)
        Cout << a[i] << “\t”;
    Return 0;
}
```

Что делает:
Вводит массив из n элементов (макс. 20), заменяет все отрицательные элементы на 0, затем выводит результат.
