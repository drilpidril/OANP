15.	Освобождение памяти двумерного динамического массива

```cpp
For (int i = 0; i < 4; ++i)
    Delete [] b[i];
Delete [] b;
```

Что делает:
Корректно освобождает память, выделенную под двумерный массив: сначала каждую строку, затем массив указателей.
