6.	Вывод элементов массива через индексы

```cpp
For (int i = 0; i < 10; i++) // вывод элементов массива на экран
    Cout << “b[“ << i << “] = “ << b[i] << “\n”;
```

Что делает:
Выводит все элементы массива b на экран в виде b[индекс] = значение.
